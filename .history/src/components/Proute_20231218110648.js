import React from 'react'
import { UserAuth } from '../context/Auth'

const Proute = ({children}) => {
    if(!user) {
        return <Naviga
    }

  return (
    <div>Proute</div>
  )
}

export default Proute